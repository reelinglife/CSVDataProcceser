<Window x:Class="CsvDataProcessor.Views.AddRecordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="データの新規追加" MinHeight="480" Height="480" Width="350" MinWidth="350" MaxHeight="500" MaxWidth="400"
        WindowStartupLocation="CenterOwner"
        FontFamily="{StaticResource MaterialDesignFont}"
        xmlns:vm="clr-namespace:CsvDataProcessor.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:AddRecordViewModel, IsDesignTimeCreatable=True}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <TextBlock Text="新規データ入力" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,20"/>
                <DatePicker md:HintAssist.Hint="日付" SelectedDate="{Binding Date}" Style="{StaticResource MaterialDesignFloatingHintDatePicker}" Margin="0,10"/>
                <TextBox md:HintAssist.Hint="商品名" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,10">
                    <TextBox.Text>
                        <Binding Path="ProductName" UpdateSourceTrigger="PropertyChanged" ValidatesOnDataErrors="True" />
                    </TextBox.Text>
                </TextBox>
                <TextBox md:HintAssist.Hint="カテゴリ" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,10">
                    <TextBox.Text>
                        <Binding Path="Category" UpdateSourceTrigger="PropertyChanged" ValidatesOnDataErrors="True" />
                    </TextBox.Text>
                </TextBox>
                <TextBox md:HintAssist.Hint="単価" Text="{Binding Price}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,10"/>
                <TextBox md:HintAssist.Hint="数量" Text="{Binding Quantity}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,10"/>
            </StackPanel>
        </ScrollViewer>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,20,20">
            <Button Content="キャンセル" IsCancel="True" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,10,0"/>
            <Button Content="追加" Command="{Binding AddCommand}" Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>